tamanu_source_dbt:
  target: demo
  outputs:
    demo:
      type: postgres
      threads: 1
      host: "{{ env_var('TAMANU_DL_DB_URL') }}"
      port: 5432
      user: "{{ env_var('TAMANU_DL_DB_USER') }}"
      pass: "{{ env_var('TAMANU_DL_DB_PASSWORD') }}"
      dbname: "{{ env_var('TAMANU_DL_DB_DATABASE') }}"
      schema: reporting
    release:
      type: postgres
      threads: 1
      host: "{{ env_var('TAMANU_RL_DB_URL') }}"
      port: "{{ env_var('TAMANU_RL_DB_PORT') }}"
      user: "{{ env_var('TAMANU_RL_DB_USER') }}"
      pass: "{{ env_var('TAMANU_RL_DB_PASSWORD') }}"
      dbname: "{{ env_var('TAMANU_RL_DB_DATABASE') }}"
      schema: reporting
    