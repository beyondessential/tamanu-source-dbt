{
  "query": "replace this",
  "name": "Sensitive audit - outpatient appointments",
  "status": "published",
  "notes": "This report shows an audit log of all modifications and cancellations to outpatient appointments at sensitive facilities. Each row represents a change event, showing both the current appointment details and the previous values before the change. The report is designed to track which Tamanu users are creating and editing appointments. Status-only changes (e.g., Confirmed to Arrived) are excluded unless the status change is to Cancelled. If an appointment is changed multiple times, each modification will appear as a separate line. The date range filters when a change was recorded, not when the appointment is scheduled.",
  "dbSchema": "reporting",
  "queryOptions": {
    "defaultDateRange": "24hours",
    "dataSources": [
      "thisFacility",
      "allFacilities"
    ],
    "parameters": [
      {
        "parameterField": "FacilityField",
        "label": "Facility",
        "name": "facilityId",
        "filterBySelectedFacility": true
      }
    ]
  }
}
