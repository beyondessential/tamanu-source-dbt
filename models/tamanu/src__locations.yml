version: 2

sources:
  - name: tamanu
    schema: public
    description: "Tamanu source database"
    tables:
      - name: locations
        description: "{{ doc('table__locations') }}"
        columns:
          - name: id
            data_type: CHARACTER VARYING(255)
            description: "{{ doc('locations__id') }}"
            tests:
              - unique
              - not_null
          - name: created_at
            data_type: TIMESTAMP WITH TIME ZONE
            description: "{{ doc('generic__created_at') }} in locations"
            tests:
              - not_null
          - name: updated_at
            data_type: TIMESTAMP WITH TIME ZONE
            description: "{{ doc('generic__updated_at') }} in locations"
            tests:
              - not_null
          - name: deleted_at
            data_type: TIMESTAMP WITH TIME ZONE
            description: "{{ doc('generic__deleted_at') }} in locations"
          - name: code
            data_type: CHARACTER VARYING(255)
            description: "{{ doc('locations__code') }}"
            tests:
              - not_null
          - name: name
            data_type: CHARACTER VARYING(255)
            description: "{{ doc('locations__name') }}"
            tests:
              - not_null
          - name: facility_id
            data_type: CHARACTER VARYING(255)
            description: "{{ doc('facilities__id') }}"
            tests:
              - relationships:
                  to: source('tamanu', 'facilities')
                  field: id
          - name: visibility_status
            data_type: TEXT
            description: "{{ doc('generic__visibility_status') }} in locations, default value is 'current'"
            tests:
              - not_null
              - accepted_values:
                  values: ["current", "historical"]
          - name: location_group_id
            data_type: CHARACTER VARYING(255)
            description: "{{ doc('location_groups__id') }}"
            tests:
              - relationships:
                  to: source('tamanu', 'location_groups')
                  field: id
          - name: max_occupancy
            data_type: INTEGER
            description: "{{ doc('locations__max_occupancy') }}"
          - name: updated_at_sync_tick
            data_type: BIGINT
            description: "{{ doc('generic__updated_at_sync_tick') }} locations"
            tests:
              - not_null
