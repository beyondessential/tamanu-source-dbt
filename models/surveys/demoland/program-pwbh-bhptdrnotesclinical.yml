version: 2

models:
  - name: program-pwbh-bhptdrnotesclinical
    description: "Dataset containing responses for 'BH outpatient doctor notes - clinical' survey"
    columns:
      - name: encounter_id
        description: '{{ doc("survey_responses__encounter_id") }}'
      - name: response_id
        description: '{{ doc("generic__id") }} in survey_responses.'
      - name: patient_id
        description: '{{ doc("encounters__patient_id") }}'
      - name: start_datetime
        description: '{{ doc("survey_responses__start_time") }}'
      - name: result_text
        description: '{{ doc("survey_responses__result_text") }}'
      - name: BHOUTPTDRC002
        description: "Doctors notes"
