version: 2
sources:
  - name: tamanu
    schema: public
    description: "{{ doc('generic__schema') }}"
    tables:
      - name: patient_invoice_insurance_plans
        description: "{{ doc('table__patient_invoice_insurance_plans') }}"
        config:
          tags: []
          meta:
            triggers:
              - notify_patient_invoice_insurance_plans_changed
              - record_patient_invoice_insurance_plans_changelog
              - set_patient_invoice_insurance_plans_updated_at
              - set_patient_invoice_insurance_plans_updated_at_sync_tick
        columns:
          - name: id
            data_type: text
            description: "{{ doc('generic__id') }} in patient_invoice_insurance_plans."
            data_tests:
              - unique
              - not_null
            config:
              meta:
                masking: text
          - name: created_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__created_at') }} in patient_invoice_insurance_plans."
            data_tests:
              - not_null
          - name: updated_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__updated_at') }} in patient_invoice_insurance_plans."
            data_tests:
              - not_null
          - name: deleted_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__deleted_at') }} in patient_invoice_insurance_plans."
          - name: patient_id
            data_type: text
            description: "{{ doc('patient_invoice_insurance_plans__patient_id') }}"
            data_tests:
              - not_null
            config:
              meta:
                masking: text
          - name: invoice_insurance_plan_id
            data_type: text
            description: "{{ doc('patient_invoice_insurance_plans__invoice_insurance_plan_id') }}"
            data_tests:
              - not_null
            config:
              meta:
                masking: text
          - name: updated_at_sync_tick
            data_type: bigint
            description: "{{ doc('generic__updated_at_sync_tick') }} in patient_invoice_insurance_plans."
            data_tests:
              - not_null
