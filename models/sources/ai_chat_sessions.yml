version: 2
sources:
  - name: tamanu
    schema: public
    description: "{{ doc('generic__schema') }}"
    tables:
      - name: ai_chat_sessions
        description: "{{ doc('table__ai_chat_sessions') }}"
        config:
          tags: []
          meta:
            triggers: []
        columns:
          - name: id
            data_type: character varying(255)
            description: "{{ doc('generic__id') }} in ai_chat_sessions."
            data_tests:
              - unique
              - not_null
          - name: created_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__created_at') }} in ai_chat_sessions."
            data_tests:
              - not_null
          - name: updated_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__updated_at') }} in ai_chat_sessions."
            data_tests:
              - not_null
          - name: deleted_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__deleted_at') }} in ai_chat_sessions."
          - name: context_name
            data_type: text
            description: "{{ doc('ai_chat_sessions__context_name') }}"
            data_tests:
              - not_null
            config:
              meta:
                masking: text
          - name: messages
            data_type: jsonb
            description: "{{ doc('ai_chat_sessions__messages') }}"
            data_tests:
              - not_null
          - name: expires_at
            data_type: timestamp with time zone
            description: "{{ doc('ai_chat_sessions__expires_at') }}"
            data_tests:
              - not_null
