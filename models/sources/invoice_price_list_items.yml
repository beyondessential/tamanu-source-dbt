version: 2
sources:
  - name: tamanu
    schema: public
    description: "{{ doc('generic__schema') }}"
    tables:
      - name: invoice_price_list_items
        description: "{{ doc('table__invoice_price_list_items') }}"
        config:
          tags: []
          meta:
            triggers:
              - notify_invoice_price_list_items_changed
              - record_invoice_price_list_items_changelog
              - set_invoice_price_list_items_updated_at
              - set_invoice_price_list_items_updated_at_sync_tick
        columns:
          - name: id
            data_type: text
            description: "{{ doc('generic__id') }} in invoice_price_list_items."
            data_tests:
              - unique
              - not_null
            config:
              meta:
                masking: text
          - name: created_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__created_at') }} in invoice_price_list_items."
            data_tests:
              - not_null
          - name: updated_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__updated_at') }} in invoice_price_list_items."
            data_tests:
              - not_null
          - name: deleted_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__deleted_at') }} in invoice_price_list_items."
          - name: updated_at_sync_tick
            data_type: bigint
            description: "{{ doc('generic__updated_at_sync_tick') }} in invoice_price_list_items."
            data_tests:
              - not_null
          - name: invoice_price_list_id
            data_type: text
            description: "{{ doc('invoice_price_list_items__invoice_price_list_id') }}"
            data_tests:
              - not_null
            config:
              meta:
                masking: text
          - name: invoice_product_id
            data_type: text
            description: "{{ doc('invoice_price_list_items__invoice_product_id') }}"
            data_tests:
              - not_null
            config:
              meta:
                masking: text
          - name: price
            data_type: numeric
            description: "{{ doc('invoice_price_list_items__price') }}"
