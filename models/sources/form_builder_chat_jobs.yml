version: 2
sources:
  - name: tamanu
    schema: public
    description: "{{ doc('generic__schema') }}"
    tables:
      - name: form_builder_chat_jobs
        description: "{{ doc('table__form_builder_chat_jobs') }}"
        config:
          tags: []
          meta:
            triggers: []
        columns:
          - name: id
            data_type: character varying(255)
            description: "{{ doc('generic__id') }} in form_builder_chat_jobs."
            data_tests:
              - unique
              - not_null
          - name: created_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__created_at') }} in form_builder_chat_jobs."
            data_tests:
              - not_null
          - name: updated_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__updated_at') }} in form_builder_chat_jobs."
            data_tests:
              - not_null
          - name: deleted_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__deleted_at') }} in form_builder_chat_jobs."
          - name: user_id
            data_type: character varying(255)
            description: "{{ doc('form_builder_chat_jobs__user_id') }}"
            data_tests:
              - not_null
          - name: status
            data_type: text
            description: "{{ doc('form_builder_chat_jobs__status') }}"
            data_tests:
              - not_null
            config:
              meta:
                masking: text
          - name: result
            data_type: jsonb
            description: "{{ doc('form_builder_chat_jobs__result') }}"
          - name: error
            data_type: jsonb
            description: "{{ doc('form_builder_chat_jobs__error') }}"
          - name: expires_at
            data_type: timestamp with time zone
            description: "{{ doc('form_builder_chat_jobs__expires_at') }}"
            data_tests:
              - not_null
