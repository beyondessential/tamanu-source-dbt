version: 2
sources:
  - name: tamanu
    schema: public
    description: "{{ doc('generic__schema') }}"
    tables:
      - name: location_assignments
        description: "{{ doc('table__location_assignments') }}"
        config:
          tags: []
          meta:
            triggers:
              - notify_location_assignments_changed
              - record_location_assignments_changelog
              - set_location_assignments_updated_at
              - set_location_assignments_updated_at_sync_tick
        columns:
          - name: id
            data_type: uuid
            description: "{{ doc('generic__id') }} in location_assignments."
            data_tests:
              - unique
              - not_null
          - name: user_id
            data_type: character varying(255)
            description: "{{ doc('location_assignments__user_id') }}"
            data_tests:
              - not_null
          - name: location_id
            data_type: character varying(255)
            description: "{{ doc('location_assignments__location_id') }}"
            data_tests:
              - not_null
          - name: date
            data_type: character(10)
            description: "{{ doc('generic__date') }} in location_assignments."
            data_tests:
              - not_null
          - name: start_time
            data_type: time without time zone
            description: "{{ doc('location_assignments__start_time') }}"
            data_tests:
              - not_null
          - name: end_time
            data_type: time without time zone
            description: "{{ doc('location_assignments__end_time') }}"
            data_tests:
              - not_null
          - name: template_id
            data_type: uuid
            description: "{{ doc('location_assignments__template_id') }}"
          - name: created_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__created_at') }} in location_assignments."
            data_tests:
              - not_null
          - name: updated_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__updated_at') }} in location_assignments."
            data_tests:
              - not_null
          - name: deleted_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__deleted_at') }} in location_assignments."
          - name: updated_at_sync_tick
            data_type: bigint
            description: "{{ doc('generic__updated_at_sync_tick') }} in location_assignments."
            data_tests:
              - not_null
