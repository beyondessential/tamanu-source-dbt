version: 2
sources:
  - name: tamanu
    schema: public
    description: "{{ doc('generic__schema') }}"
    tables:
      - name: invoice_price_lists
        description: "{{ doc('table__invoice_price_lists') }}"
        config:
          tags: []
          meta:
            triggers:
              - notify_invoice_price_lists_changed
              - record_invoice_price_lists_changelog
              - set_invoice_price_lists_updated_at
              - set_invoice_price_lists_updated_at_sync_tick
        columns:
          - name: id
            data_type: text
            description: "{{ doc('generic__id') }} in invoice_price_lists."
            data_tests:
              - unique
              - not_null
            config:
              meta:
                masking: text
          - name: created_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__created_at') }} in invoice_price_lists."
            data_tests:
              - not_null
          - name: updated_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__updated_at') }} in invoice_price_lists."
            data_tests:
              - not_null
          - name: deleted_at
            data_type: timestamp with time zone
            description: "{{ doc('generic__deleted_at') }} in invoice_price_lists."
          - name: updated_at_sync_tick
            data_type: bigint
            description: "{{ doc('generic__updated_at_sync_tick') }} in invoice_price_lists."
            data_tests:
              - not_null
          - name: code
            data_type: character varying(255)
            description: "{{ doc('invoice_price_lists__code') }}"
            data_tests:
              - not_null
          - name: name
            data_type: character varying(255)
            description: "{{ doc('invoice_price_lists__name') }}"
          - name: rules
            data_type: jsonb
            description: "{{ doc('invoice_price_lists__rules') }}"
          - name: visibility_status
            data_type: text
            description: "{{ doc('generic__visibility_status') }} in invoice_price_lists."
            data_tests:
              - not_null
            config:
              meta:
                masking: text
