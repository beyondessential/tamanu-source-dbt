version: 2
models:
  - name: lab_requests_metadata
    description: "{{ doc('generic__metadata') }} for lab_requests."
    config:
      tags:
        - clinical
        - log
    meta:
      owner: bes-maui
      domain: clinical
      tier: silver
      pii: false
      classification: restricted
    columns:
      - name: id
        data_type: character varying(255)
        description: "{{ doc('generic__id') }} in lab_requests."
      - name: created_datetime
        data_type: timestamp
        description: "{{ doc('generic__created_at') }} in the lab_requests table."
      - name: updated_datetime
        data_type: timestamp
        description: "{{ doc('generic__updated_at') }} in the lab_requests table."

