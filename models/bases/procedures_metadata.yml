version: 2
models:
  - name: procedures_metadata
    description: "{{ doc('generic__metadata') }} for procedures."
    config:
      tags:
        - clinical
        - log
    columns:
      - name: id
        data_type: character varying(255)
        description: "{{ doc('generic__id') }} in procedures."
      - name: created_datetime
        data_type: timestamp
        description: "{{ doc('generic__created_at') }} in the procedures table."
      - name: updated_datetime
        data_type: timestamp
        description: "{{ doc('generic__updated_at') }} in the procedures table."

