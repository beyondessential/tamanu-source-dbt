version: 2
models:
  - name: users_with_metadata
    description: "{{ doc('generic__metadata') }} for users."
    config:
      tags:
        - administration
        - log
    columns:
      - name: id
        data_type: character varying(255)
        description: "{{ doc('generic__id') }} in users."
      - name: created_datetime
        data_type: timestamp
        description: "{{ doc('generic__created_at') }} in the users table."
      - name: updated_datetime
        data_type: timestamp
        description: "{{ doc('generic__updated_at') }} in the users table."

