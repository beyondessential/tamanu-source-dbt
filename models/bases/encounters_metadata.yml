version: 2
models:
  - name: encounters_metadata
    description: "{{ doc('generic__metadata') }} for encounters."
    config:
      tags:
        - clinical
        - log
    meta:
      owner: bes-maui
      domain: clinical
      pii: false
      classification: restricted
    columns:
      - name: id
        data_type: character varying(255)
        description: "{{ doc('generic__id') }} in encounters."
      - name: created_datetime
        data_type: timestamp
        description: "{{ doc('generic__created_at') }} in the encounters table."
      - name: updated_datetime
        data_type: timestamp
        description: "{{ doc('generic__updated_at') }} in the encounters table."

