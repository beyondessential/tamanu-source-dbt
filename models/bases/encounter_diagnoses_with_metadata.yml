version: 2
models:
  - name: encounter_diagnoses_with_metadata
    description: "{{ doc('generic__metadata') }} for encounter_diagnoses."
    config:
      tags:
        - clinical
        - log
    columns:
      - name: id
        data_type: character varying(255)
        description: "{{ doc('generic__id') }} in encounter_diagnoses."
      - name: created_datetime
        data_type: timestamp
        description: "{{ doc('generic__created_at') }} in the encounter_diagnoses table."
      - name: updated_datetime
        data_type: timestamp
        description: "{{ doc('generic__updated_at') }} in the encounter_diagnoses table."

