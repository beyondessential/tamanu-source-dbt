name: tamanu_source_dbt
version: "2.20.0"
config-version: 2

profile: tamanu_source_dbt

vars:
  language: en
  test_patient: h1627394-3778-4c31-a510-9fcb88efdbf3

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - compiled
  - dbt_packages
  - target

models:
  tamanu_source_dbt:
    bases:
      +materialized: view
      +tags: [fiji, palau, samoa, demoland]

    datasets:
      +materialized: view
      fiji:
        +tags: [fiji]
      generic:
        +tags: [fiji, palau, samoa, demoland]

    translated:
      +materialized: view
      fiji:
        +tags: [fiji]
      generic:
        +tags: [fiji, palau, samoa, demoland]

    reports:
      +tags: [reports]
      sql:
        fiji:
          +tags: [fiji]
        generic:
          +tags: [fiji, palau, samoa, demoland]

data_tests:
  +severity: warn
