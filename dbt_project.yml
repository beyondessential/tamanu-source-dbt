name: "tamanu_source_dbt"
version: "2.16"
config-version: 2


profile: "tamanu_source_dbt"


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]


clean-targets:
  - "target"
  - "dbt_packages"


models:
  tamanu_source_dbt:
    reporting:
      +materialized: view
      +schema: reporting

data_tests:
  +severity: warn